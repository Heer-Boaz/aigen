[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aigen"
version = "0.1.0"
description = "Private AI character keyframe generation pipeline."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Proprietary" }
authors = [{ name = "Boaz" }]
dependencies = [
  "huggingface_hub>=0.26",
  "numpy>=2",
  "Pillow>=10",
  "pydantic>=2",
  "scipy>=1.18",
]

[project.optional-dependencies]
generation = [
  "accelerate>=1",
  "bitsandbytes>=0.46.1",
  "comfy-kitchen==0.2.20",
  "controlnet-dwpose==0.1.5",
  "controlnet-aux==0.0.10",
  "datasets>=2",
  "diffusers[torch] @ git+https://github.com/huggingface/diffusers.git",
  "einops>=0.8",
  "fairscale>=0.4.13",
  "ftfy",
  "Jinja2",
  "kernels==0.12.3",
  "opencv-python-headless>=4.10",
  "onnxruntime-gpu==1.26.0",
  "peft>=0.17",
  "protobuf",
  "qwen-vl-utils==0.0.8",
  "safetensors>=0.4",
  "scikit-learn>=1.5",
  "segment-anything==1.0",
  "sentencepiece",
  "spandrel>=0.4",
  "tensorboard",
  "timm>=1.0.11",
  "torch==2.12.1",
  "transformers>=4.44",
  "torchvision==0.27.1",
  "tqdm>=4.66",
]

[project.scripts]
aigen = "aigen.cli:main"

[tool.setuptools]
packages = ["aigen", "aigen.generation", "aigen.models"]
