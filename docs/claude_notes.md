# Superseded — see docs/PLAN.md

This file previously duplicated the implementation blueprint. It is now
superseded: **`docs/PLAN.md` is the single authoritative blueprint** for the
local PixAI-Edit-Pro-style character edit pipeline, including the defect
response ladder (single-pass first), the audit loop (stage 10 / M5), the
verification matrix, the resolution policy, the structural-source composition
and pose-proportion rules, the backbone candidates (2509 baseline /
2511-LightX2V / FLUX.2 klein), and the hard rules. `AGENTS.md` behavioral rules still hold.

Do not edit this file; amend `docs/PLAN.md` instead.
